language: generic 
sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t francoser/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true francoser/docker-react npm run test